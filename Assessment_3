Welcome to assessment 3 guys 

Question 1:Get a number from user and see if the number equals to 50 .If yes the print success otherwise print Failure

 #include <stdio.h>
int main() {
    int x;
    scanf("%d",&x);
    if(x==50)
    {
        printf("Success");
    }
    else
     printf("Failure");
    return 0;
}

Question 2:Get a number from user and see if the number is not equal to 50 .If yes the print success otherwise print Failure

#include <stdio.h>
int main() {
    int x;
    scanf("%d",&x);
    if(x!=50)
    {
        printf("Success");
    }
    else
     printf("Failure");
    return 0;
}

Question 3:Get a number from user and see if the number is less than 50 .If yes the print success otherwise print Failure

#include <stdio.h>
int main() {
    int x;
    scanf("%d",&x);
    if(x<50)
    {
        printf("Success");
    }
    else
     printf("Failure");
    return 0;
}

Question 4:Get a number from user and see if the number is greater than 50 .If yes the print success otherwise print Failure

#include <stdio.h>
int main() {
    int x;
    scanf("%d",&x);
    if(x>50)
    {
        printf("Success");
    }
    else
     printf("Failure");
    return 0;
}

Question 5:Get a number from user and see if the number is less than or equal to 50 .If yes the print success otherwise print Failure

#include <stdio.h>
int main() {
    int x;
    scanf("%d",&x);
    if(x<=50)
    {
        printf("Success");
    }
    else
     printf("Failure");
    return 0;
}

Question 6:Get a number from user and see if the number is greater than or equal to 50 .If yes the print success otherwise print Failure

#include<stdio.h>
int main ( ){
    int x;
    scanf("%d",&x);
    if (x>=50){
        printf("success");
    }
    else
    printf("failure");
    return 0;
}

Question 7:Get a two digit number from the user and check if its digit 0 and 1 are equal.If yes the print success otherwise print Failure

#include<stdio.h>
int main(){
    int x,y;
    scanf("%d",&x);
    y=x/10;
    x=x%10;
    if(y==x){
        printf("success");
    }
    else
    printf("failure");
    return 0;
}

Question 8: Get a two digit number from the user and check if its digit 0 and 1 are not equal.If yes the print success otherwise print Failure

#include<stdio.h>
int main(){
    int x,y;
    scanf("%d",&x);
    y=x/10;
    x=x%10;
    if(y!=x){
        printf("success");
    }
    else
    printf("failure");
    return 0;
}

Question 9: Get a two digit number from the user and check if its digit 0 is less than digit 1.If yes the print success otherwise print Failure

#include<stdio.h>
int main(){
    int x,y;
    scanf("%d",&x);
    y=x/10;
    x=x%10;
    if(x<y){
        printf("success");
    }
    else
    printf("failure");
    return 0;
}

Question 10: Get a two digit number from the user and check if its digit 0 is greater than digit 1.If yes the print success otherwise print Failure

#include<stdio.h>
int main(){
    int x,y;
    scanf("%d",&x);
    y=x/10;
    x=x%10;
    if(x>y){
        printf("success");
    }
    else 
    printf("failure");
    return 0;
}

Question 11: Get a two digit number from the user and check if its digit 1 is less than or equal to digit 0.If yes the print success otherwise print Failure

#include<stdio.h>
int main(){
    int x,y;
    scanf("%d",&x);
    y=x/10;
    x=x%10;
    if(y<=x){
        printf("success");
    
    }
    else 
    printf("failure");
    return 0 ;  
}

Question 12: Get a two digit number from the user and check if its digit 1 is greater than or equal to digit 0.If yes the print success otherwise print Failure

#include<stdio.h>
int main (){
    int x,y;
    scanf("%d",&x);
    y=x/10;
    x=x%10;
    if(y>=x){
        printf("success");
    }
    else 
    printf("failure");
    return 0;
}

Question 13: Get a three digit number from the user and check if its digit 1 and digit 0 are equal and digit 1 and digit 2 are different.If yes the print success otherwise print Failure

#include<stdio.h>
int main(){
    int x,y,z;
    scanf("%d",&x);
    y=x/10;
    z=y/10;
    y=y%10;
    x=x%10;
    x=y/x;
    z=z*x;
    y=y*x;
    if(z!=y){
        printf("success");
        }
    else 
    printf("failure");
    return 0;
}

Question 14: Get a four digit number from the user and check if its first 2 digits and last 2 digits are same .If yes the print success otherwise print Failure

#include<stdio.h>
int main(){
    int w,x,y,z;
    scanf("%d",&x);
    z=x/1000;//d-3
    w=x/100;
    w=w%10;//d-2
    y=x%100;
    y=y/10;//d-1
    x=x%10;//d-0
    y=((z*1000)/(y));
    x=((w*1000)/(x));
    y=y-x;
    printf("%d\n",y);
    if(y==0){
        printf("success");
    }
    else
    printf("failure");
    return 0;
}

Finished


































